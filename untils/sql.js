// // 传入mysqp分页参数设置
// function setPagination(req) {
//   let [pageIndex, pageSize] = global.$getArgs(req, 'pageIndex', 'pageSize')
//   pageIndex = parseInt(pageIndex)
//   pageSize = parseInt(pageSize)
//   if (pageIndex !== 1) {
//     pageIndex = pageIndex * pageSize
//   } 
//   return [pageIndex, pageSize]
// }

